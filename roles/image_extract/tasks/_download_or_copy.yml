---
- name: Detect source flavour
  ansible.builtin.set_fact:
    __image_extract_is_url: "{{ armbian_image_src is match('^https?://') }}"

- name: Download .img.xz from URL
  ansible.builtin.get_url:
    url: "{{ armbian_image_src }}"
    dest: "{{ __image_extract_img_cache }}"
    mode: "0644"
    timeout: 600
  when: __image_extract_is_url

- name: Copy .img.xz from local path
  ansible.builtin.copy:
    src: "{{ armbian_image_src }}"
    dest: "{{ __image_extract_img_cache }}"
    remote_src: true
    mode: "0644"
  when: not __image_extract_is_url
