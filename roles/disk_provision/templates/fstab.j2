{{ ansible_managed | comment }}
# Generated by david_igou.armbian_netboot.disk_provision.
# Mirrors armbian-install's "fresh fstab from scratch" approach: just
# the root entry + tmpfs for /tmp. Any extra mounts (NFS, bind, swap)
# must be added by a downstream task or operator.
#
# <file system>	<mount point>	<type>	<options>	<dump>	<pass>
tmpfs	/tmp	tmpfs	defaults,nosuid	0	0
LABEL={{ label }}	/	{{ fstype }}	{{ mount_opts }}	0	1
